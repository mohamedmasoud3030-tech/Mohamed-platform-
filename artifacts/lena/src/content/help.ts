/**
 * Public help content.
 *
 * The canonical authority lives in `@workspace/content` so the public site and
 * the API (LENA Assistant grounding) consume one copy of the verified answers.
 * This file stays as the stable `@/content/help` import path for the app.
 */

export * from "@workspace/content";
